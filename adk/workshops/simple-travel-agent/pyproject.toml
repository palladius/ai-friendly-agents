[project]
name = "simple-travel-agent-workshop"
version = "0.2.0"
description = "Simple Travel Agent Workshop using Google ADK"
requires-python = ">=3.11"
dependencies = [
    "google-adk==1.19.0",
    "mcp",
    "nanobanana-mcp-server",
    "requests", # New dependency for web_fetch_curl tool
]