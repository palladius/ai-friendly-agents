[GitHub Issue #9](https://github.com/palladius/ai-friendly-agents/issues/9)

# 🇫🇷 Resize Workshop Images

The user wants to resize the "cute robot images" in `WORKSHOP.md` to be 50% width and inline with the text, to fill empty spaces better.

- [ ] **Resize Images in `WORKSHOP.md`**
    - [ ] Identify all image references (e.g., `![alt text](image-X.png)`).
    - [ ] Convert standard Markdown image syntax to HTML `<img>` tags to support resizing.
    - [ ] Set `width="50%"` and `align="right"` (or left, depending on flow) for a better layout.
    - [ ] Ensure the text wraps around them nicely.

- [ ] **Verify**
    - [ ] Check the markdown rendering (mentally or via preview if possible).
